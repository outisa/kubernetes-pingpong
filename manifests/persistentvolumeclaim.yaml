apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: mainapp-namespace
  name: pongs-claim
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi